## Gradient Descent Viz — Vite edition

This is the **Vue 3 + Vite** version of the web app.

### Run (dev)
```bash
cd web
npm install
npm run dev
```

### Milestone status
- **M0 (Hello viz) complete**:
  - 3D surface renders
  - Vanilla Gradient Descent animates
  - Path line is drawn
  - Minimal UI: play/pause, reset, wireframe toggle
